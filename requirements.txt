# Shared runtime dependencies for scripts in this collection.
# A script needing something beyond this should document that extra
# dependency in a comment at the top of the script itself, not here.

pandas>=2.0.0
python-dotenv>=1.0.0
