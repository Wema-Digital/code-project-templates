# Runtime dependencies, pinned tightly — ML reproducibility depends on
# exact versions more than most templates (numeric results, model
# behaviour, and even plot output can shift between minor versions).

pandas==3.0.3
scikit-learn==1.8.0
numpy==2.4.4
matplotlib==3.10.9
