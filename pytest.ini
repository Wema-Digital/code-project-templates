[pytest]
; Puts the project root on sys.path before collection -- without this, plain
; `pytest` (what this template's own CI runs) fails to import top-level
; packages like this one's. `python -m pytest` masked this locally since -m
; adds cwd to sys.path itself; the bare `pytest` entrypoint does not.
pythonpath = .
